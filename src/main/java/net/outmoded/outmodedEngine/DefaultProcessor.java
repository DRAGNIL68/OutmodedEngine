package net.outmoded.outmodedEngine;

import net.outmoded.outmodedEngine.templates.ModelTemplate;

/**
 * this must be done async
 */
public class DefaultProcessor implements JsonProcessor {
    @Override
    public ModelTemplate process(String json) {
        // extra stuff
        // load default nodes
        // load variants
        // load animations
        // store pack data


        return null;
    }
}
